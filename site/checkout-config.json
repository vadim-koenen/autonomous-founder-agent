{
  "status": "pending",
  "experiment_id": "",
  "provider": "",
  "checkout_url": "",
  "configured_by_human": false,
  "notes": "No payment rail is active. Configure only the public checkout URL for a selected, validated experiment; never store API keys or secrets here."
}
