[project]
name = "autonomous-founder-agent"
version = "0.1.0"
description = "M1 non-executing decision system for an autonomous revenue-seeking founder agent."
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
