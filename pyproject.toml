[project]
name = "autonomous-founder-agent"
version = "0.4.0"
description = "Continuous discovery and bounded execution for an autonomous founder agent."
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
