[project]
name = "autonomous-founder-agent"
version = "0.3.0"
description = "Continuous evidence-led revenue operator for an autonomous founder agent."
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
